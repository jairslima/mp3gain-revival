# Audit Report - 2026-03-27

This document records the repository state observed on 2026-03-27 in the workspace `C:\Users\jairs\Claude\MP3Gain`.

## Remediation Status

- This report captures the issues found during the audit pass.
- Later on the same date, the following items were corrected in the workspace:
  - `test/smoke_test.sh` path handling under `bash`/WSL when selecting `build/Release/mp3gain.exe`
  - `test/probe_corruptions.ps1` Linux-binary detection and result regeneration
  - GUI target-volume baseline alignment to `95.5 dB`
  - stale release/handoff documentation in the main operational docs
- Keep reading this file as a record of what was found, not as the final post-fix status snapshot.

## Scope

- Repository structure and workspace hygiene
- Current documentation accuracy
- Active CLI build/test paths
- GUI wrapper alignment with the CLI
- Corruption-probe reproducibility

## Validation Performed

- Read the current top-level and `docs/` documentation
- Inspected the active source in `project/` and the Avalonia GUI wrapper in `gui/`
- Executed the Windows CLI directly:
  - `.\build\Release\mp3gain.exe .\test\fixtures\test1.mp3`
- Executed:
  - `bash test/smoke_test.sh`
  - `pwsh -File test\probe_corruptions.ps1`

## Current Workspace Snapshot

- `build\Release\mp3gain.exe` exists
- `build\Release\mpg123.dll` exists
- `build\Release\mp3gain.exe` responds correctly when run from PowerShell
- The git worktree contains untracked/generated residue:
  - `build-wsl/`
  - `CUsersjairs.claude_speedtest`
  - a second untracked path reported by `git status` as `"C\357\200\272Usersjairs.claude_speedtest"`

## Findings

### 1. High - `test/smoke_test.sh` is not portable across the mixed Windows/WSL path it currently advertises

Evidence:
- [test/smoke_test.sh](C:\Users\jairs\Claude\MP3Gain\test\smoke_test.sh):8 selects `build/Release/mp3gain.exe` when present.
- [test/smoke_test.sh](C:\Users\jairs\Claude\MP3Gain\test\smoke_test.sh):16 creates a temp dir with `mktemp -d`.
- [test/smoke_test.sh](C:\Users\jairs\Claude\MP3Gain\test\smoke_test.sh):17-19 place test files under that Unix temp dir.

Impact:
- When the script picks the Windows executable and then passes Unix temp paths such as `/tmp/...`, the test target and the temp-path model are mismatched.
- In this workspace, `bash test/smoke_test.sh` reported `5/27 passed`, while the same CLI command worked correctly from PowerShell against the fixture path.

Observed behavior:
- The direct Windows invocation returned the expected track and album analysis output.
- The smoke script produced blank assertion outputs for most file-based checks, which is consistent with the selected executable and test-file path model not matching.

Conclusion:
- The current "`27/27 passed`" claim is not reproducible from this workspace with the documented command.

### 2. High - `test/probe_corruptions.ps1` is currently validating against a Linux binary path that does not exist here

Evidence:
- [test/probe_corruptions.ps1](C:\Users\jairs\Claude\MP3Gain\test\probe_corruptions.ps1):7 hardcodes:
  - `"/mnt/c/Users/jairs/Claude/MP3Gain/build/mp3gain"`
- [test/probe_corruptions.ps1](C:\Users\jairs\Claude\MP3Gain\test\probe_corruptions.ps1):54 invokes that hardcoded path via `wsl.exe`.
- [test/probe_corruptions.results.json](C:\Users\jairs\Claude\MP3Gain\test\probe_corruptions.results.json):5 and many later entries persist:
  - `"/bin/bash: line 1: /mnt/c/Users/jairs/Claude/MP3Gain/build/mp3gain: No such file or directory"`

Impact:
- The persisted probe matrix in `test/probe_corruptions.results.json` is not the output of the MP3Gain binary in this workspace.
- The current probe run is effectively recording only the `ffmpeg` oracle side plus a missing-binary failure on the MP3Gain side.

Conclusion:
- The current "`pwsh -File test\probe_corruptions.ps1`: OK" statements in repo docs are not reliable for this snapshot.

### 3. High - The GUI wrapper is still calibrated to `89.0 dB`, while the active tag/reference logic documents `95.5 dB`

Evidence:
- [gui/MP3GainEngine.cs](C:\Users\jairs\Claude\MP3Gain\gui\MP3GainEngine.cs):27 comments that the CLI runs around `89.0dB`
- [gui/MP3GainEngine.cs](C:\Users\jairs\Claude\MP3Gain\gui\MP3GainEngine.cs):29 computes:
  - `double mod = targetVolume - 89.0;`
- [gui/MainWindow.axaml.cs](C:\Users\jairs\Claude\MP3Gain\gui\MainWindow.axaml.cs):47 initializes:
  - `double targetVol = 89.0;`
- [PROJECT.md](C:\Users\jairs\Claude\MP3Gain\PROJECT.md):120 states:
  - `REPLAYGAIN_REFERENCE_LOUDNESS: Corrigido de 89.0 para 95.5 dB`
- [project/id3tag.c](C:\Users\jairs\Claude\MP3Gain\project\id3tag.c):1246 writes:
  - `"95.5 dB"`

Impact:
- The GUI's target-volume math is misaligned with the documented current loudness reference.
- Users can receive incorrect displayed volume calculations and incorrect `/d` offsets.

Conclusion:
- The GUI wrapper should not be considered aligned with the current CLI reference baseline.

### 4. Medium - Documentation drift is significant and currently overstates validation status

Evidence:
- [STATUS.md](C:\Users\jairs\Claude\MP3Gain\STATUS.md):10 says `bash test/smoke_test.sh`: `27/27 passed`
- [STATUS.md](C:\Users\jairs\Claude\MP3Gain\STATUS.md):11 says `pwsh -File test\probe_corruptions.ps1`: `OK`
- [NEXT_DEV_MESSAGE.md](C:\Users\jairs\Claude\MP3Gain\NEXT_DEV_MESSAGE.md):8-9 repeats the same claims
- [docs/RELEASE.md](C:\Users\jairs\Claude\MP3Gain\docs\RELEASE.md):14 says the smoke suite baseline is `27/27`
- [docs/BUILD.md](C:\Users\jairs\Claude\MP3Gain\docs\BUILD.md):41 says the active workspace baseline is `27/27`

Impact:
- A developer reading the current docs would believe the verification baseline is green in this workspace, but it is not reproducible from the current snapshot.

Conclusion:
- The release-readiness and validation docs are stale relative to the actual workspace contents.

### 5. Medium - Some documentation references files that do not exist

Evidence:
- [STATUS.md](C:\Users\jairs\Claude\MP3Gain\STATUS.md):35 references `docs/RELEASE_CANDIDATE.md`
- [NEXT_DEV_MESSAGE.md](C:\Users\jairs\Claude\MP3Gain\NEXT_DEV_MESSAGE.md):25 references `docs/RELEASE_CANDIDATE.md`
- No such file exists in the current repo tree.

Impact:
- Handoff/readiness docs point contributors to a missing artifact.

### 6. Medium - Some documentation says Windows packaging is blocked by missing local artifacts, but those artifacts exist

- [STATUS.md](C:\Users\jairs\Claude\MP3Gain\STATUS.md):24 says Windows packaging is blocked by missing:
  - `build/Release/mp3gain.exe`
  - `build/Release/mpg123.dll`
- [NEXT_DEV_MESSAGE.md](C:\Users\jairs\Claude\MP3Gain\NEXT_DEV_MESSAGE.md):12-16 says the same
- Both files exist in `build\Release\` in this workspace.

Impact:
- Handoff and next-step guidance are misleading.

### 7. Low - Root/worktree hygiene is noisy

Evidence:
- `git status --short` reports untracked/generated residue at the repository root.
- `build-wsl/` is present and not cleanly separated from the active source review path.
- There is a suspicious filename with corrupted/odd characters:
  - `CUsersjairs.claude_speedtest`

Impact:
- Repository review and release packaging can be confused by stale local artifacts.

## What Is Confirmed Working

- The Windows CLI binary exists:
  - `build\Release\mp3gain.exe`
- The required runtime DLL exists:
  - `build\Release\mpg123.dll`
- The CLI returns expected analysis output when run directly from PowerShell against the fixture:
  - `.\build\Release\mp3gain.exe .\test\fixtures\test1.mp3`

## What Is Not Reliably Confirmed By This Snapshot

- The documented `bash test/smoke_test.sh` success path
- The documented `pwsh -File test\probe_corruptions.ps1` success path
- The persisted probe matrix in `test/probe_corruptions.results.json`
- The GUI target-volume calculations

## Recommended Next Actions

1. Fix `test/smoke_test.sh` so the temp-file path model matches the selected binary:
   - either use Windows temp paths when selecting `build/Release/mp3gain.exe`
   - or force the Linux binary path when running under WSL/bash
2. Fix `test/probe_corruptions.ps1` to detect the real available binary instead of hardcoding `build/mp3gain`
3. Regenerate `test/probe_corruptions.results.json` only after the probe is hitting a real MP3Gain binary
4. Update the GUI wrapper to use the current loudness reference baseline consistently
5. Correct the stale/missing-document references in:
   - `STATUS.md`
   - `NEXT_DEV_MESSAGE.md`
   - `docs/BUILD.md`
   - `docs/RELEASE.md`
6. Clean root-level untracked/generated residue before using this workspace as a release snapshot

## Notes

- This audit did not attempt to change runtime behavior.
- This audit reflects the local workspace snapshot only; it does not prove the remote CI state.
